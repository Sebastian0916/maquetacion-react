import path from "path";
import type { RouteObject } from "react-router-dom";
import { useRoutes } from "react-router-dom";

const CambiarMaquetacion = () => {};

const RouteMaquetacion: RouteObject[] = [
  {
    children: [
      {
        path: "*",
        // element:
      },
    ],
  },
];

export default RouteMaquetacion;
